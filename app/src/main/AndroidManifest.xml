<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.paint">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Paint">
        <activity android:name=".animals.Frog"></activity>
        <activity android:name=".animals.Rabbit" />
        <activity android:name=".animals.Kitty" />
        <activity android:name=".animals.Giraffe" />
        <activity android:name=".animals.Ostrich" />
        <activity android:name=".animals.Goat" />
        <activity android:name=".Menu" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>